#labels Phase-Implementation,Featured
#summary IMAP Server command status

= Introduction =

This is a current listing of the status of IMAP commands and implimention notes to complete those commands.


|| Command || State || RFC || ERLMail || Outlook Exp || Outlokk || iPhone || Thunderbird ||
